image: daocloud/ci-node:4.2.2
services:
    - mongodb
    - redis

env:
    - MONGODB_INSTANCE_NAME = "test"
    - REDIS_PASSWORD = "zQoRgFASQ2vuhRF"
    - NODE_ENV = "production"

install:
    - npm install -g mocha

script:
    - npm install
    - mocha
