image: daocloud/ci-node:0.12
services:
    - mongodb
    - redis

env:
    - MONGODB_INSTANCE_NAME = "test"
    - REDIS_PASS = "None"

install:
    - npm install -g mocha

script:
    - npm install
    - mocha
